{% extends "base.twig" %}
{% block head %}
    <link id="theme" rel="stylesheet" type="text/css" href="{{ getAbsolutePath(["css"]) ~ "main.css" }}" />
{% endblock %}
{% block content %}
    <h3>Zeiterfassung</h3>
    <p>Willkommen, {{ display_username }}, bei der Administration von Rafisa</p>
    <p>Heute ist {{ day }}, der {{ date }}</p>
    <a href="{{ getAbsolutePath(['admin','overview',session_username,month_year]) }}" class="d-none d-md-inline">Übersicht</a>
    <br>
    <a href="{{ getAbsolutePath(['admin','edituser',session_username]) }}" class="d-none d-md-inline">Benutzer</a>
    <form action="{{ getAbsolutePath(['logout']) }}">
        <div class="my-3">
            <button type="submit" class="btn btn-primary">Abmelden</button>
        </div>
    </form>
    {% if true is same as(time_record_started) %}
        <form action="{{ getAbsolutePath(['stop']) }}">
            <div class="mb-3">
                <button type="submit" class="btn btn-primary">Zeiterfassung stoppen</button>
            </div>
        </form>
    {% else %}
        <form action="{{ getAbsolutePath(['start']) }}">
            <div class="mb-3">
                <button type="submit" class="btn btn-primary">Zeiterfassung starten</button>
            </div>
        </form>
    {% endif %}
{% endblock %}