{% extends "base.twig" %}
{% block content %}
    {{ show_form }}
    <h3>Login</h3>
    <form action="{{ action }}" method="post">
        {% if state is same as("failure") %}
            <div>
                <span class="badge bg-danger">Logindaten sind falsch.</span>
            </div>
        {% endif %}
        <div class="mb-3">
            <label for="username" class="form-label">Benutzername:</label>
            <input id="username" name="username" type="text" placeholder="m.muster" class="form-control" required>
        </div>
        <div class="mb-3">
            <label for="password" class="form-label">Passwort:</label>
            <input id="password" name="password" type="password" placeholder="Passwort" class="form-control" required>
        </div>
        <div>
            <button type="submit" class="btn btn-primary">Login</button>
        </div>
    </form>
{% endblock %}